# Copy to .env for docker-compose. Not read by the backend directly — copy the
# matching values into backend/.env (see backend/.env.example).

POSTGRES_USER=coifyn
POSTGRES_PASSWORD=coifyn
POSTGRES_DB=coifyn
POSTGRES_PORT=5432

REDIS_PORT=6379

MINIO_ROOT_USER=coifyn
MINIO_ROOT_PASSWORD=coifyn123
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025
